services:
  dev:
    image: ${DEVRIG_PROJECT:-my-project}-dev:latest
    labels:
      devrig.project: ${DEVRIG_PROJECT:-my-project}
    build:
      context: .devrig
      dockerfile: Dockerfile
      args:
        USER_UID: ${HOST_UID:-501}
      labels:
        devrig.project: ${DEVRIG_PROJECT:-my-project}
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
      - ./.devrig/home:/home/dev
    environment:
      - CLAUDE_CONFIG_DIR=/home/dev/.claude
      - BRIDGE_PORT=9229
      - CLAUDE_VERSION=${CLAUDE_VERSION:-latest}
      - TERM=xterm-256color
      - NODE_OPTIONS=--max-old-space-size=4096
    stdin_open: true
    tty: true
    init: true
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD", "bash", "-c", "command -v claude"]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 8g
          cpus: '4'

volumes:
  node_modules:
    labels:
      devrig.project: ${DEVRIG_PROJECT:-my-project}
