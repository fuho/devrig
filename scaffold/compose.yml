services:
  dev:
    image: ${DEVRIG_PROJECT:-my-project}-dev:latest
    labels:
      devrig.project: ${DEVRIG_PROJECT:-my-project}
    build:
      context: .devrig
      dockerfile: Dockerfile
      args:
        USER_UID: ${HOST_UID:-1000}
        USER_GID: ${HOST_GID:-1000}
      labels:
        devrig.project: ${DEVRIG_PROJECT:-my-project}
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
      - ./.devrig/home:/home/dev
      - ./.devrig/CLAUDE.md:/workspace/CLAUDE.md:ro
      - devrig-mask:/workspace/.devrig
    environment:
      - CLAUDE_CONFIG_DIR=/home/dev/.claude
      - BRIDGE_PORT=9229
      - BRIDGE_ENABLED=${BRIDGE_ENABLED:-0}
      - BRIDGE_VERBOSE=${BRIDGE_VERBOSE:-0}
      - CLAUDE_VERSION=${CLAUDE_VERSION:-latest}
      - TERM=xterm-256color
      - NODE_OPTIONS=--max-old-space-size=4096
    # Entrypoint runs as root to chown /home/dev, then drops to dev via gosu
    user: "root"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    stdin_open: true
    tty: true
    init: true
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD", "bash", "-c", "command -v claude"]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 120s
    deploy:
      resources:
        limits:
          memory: 8g
          cpus: '4'

volumes:
  node_modules:
    labels:
      devrig.project: ${DEVRIG_PROJECT:-my-project}
  devrig-mask:
    labels:
      devrig.project: ${DEVRIG_PROJECT:-my-project}
