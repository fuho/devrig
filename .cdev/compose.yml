services:
  dev:
    image: ${CDEV_PROJECT:-my-project}-dev:latest
    build:
      context: .cdev
      dockerfile: Dockerfile
      args:
        USER_UID: ${HOST_UID:-501}
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
      - ./.cdev/home:/home/dev
    environment:
      - CLAUDE_CONFIG_DIR=/home/dev/.claude
      - CLAUDE_INSTALL_METHOD=native
      - BRIDGE_PORT=9229
      - TERM=xterm-256color
      - NODE_OPTIONS=--max-old-space-size=4096
    stdin_open: true
    tty: true
    healthcheck:
      test: ["CMD", "bash", "-c", "command -v claude"]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 8g
          cpus: '4'

volumes:
  node_modules:
